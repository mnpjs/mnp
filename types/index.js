export {}

/* typal types/index.xml namespace */
/**
 * @typedef {_mnp.Settings} Settings
 * @typedef {Object} _mnp.Settings
 * @prop {string} token Personal access token.
 * @prop {string} org GitHub organisation.
 * @prop {string} scope NPM scope.
 * @prop {string} name Username (default is look up in git config).
 * @prop {string} email Email (default is looked up in git config).
 * @prop {string} website The websites.
 * @prop {string} trademark The trademark name for README.
 * @prop {string} legalName The official legal name for LICENSE.
 */
